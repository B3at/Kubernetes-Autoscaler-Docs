apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: protocom-calibration
  namespace: demonstrator
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard
  volumeMode: Filesystem
status: {}